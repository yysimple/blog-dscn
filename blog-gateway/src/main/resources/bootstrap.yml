spring:
  application:
    name: blog-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 39.100.24.145:5180
    gateway:
      routes:
        - id: user-center
          uri: lb://user-center
          predicates:
            - Path=/api/userCenter/**
          filters:
            - RewritePath=/api/(?<segment>.*),/$\{segment}
            - SwaggerHeaderFilter
            - StripPrefix=1

        - id: blog-center
          uri: lb://blog-center
          predicates:
            - Path=/api/blogCenter/**
          filters:
            - RewritePath=/api/(?<segment>.*),/$\{segment}
            - SwaggerHeaderFilter
            - StripPrefix=1
